export enum Type{
    INT,
    BOOLEAN,
    DOUBLE,
    STRING,
    CHAR,
    VOID
}

export enum TypeOperation{
    SUMA, RESTA, MULTIPLICACION, DIVISION, MENOSUNARIO,
    MAYOR, MENOR,
    OR, AND, NOT,
    INCREMENTO, DECREMENTO
}